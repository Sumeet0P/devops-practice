services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000  
    restart: always 

  client:
    build: ./client
    ports:
      - "5173:80"
    depends_on:
      - server
    restart: always  