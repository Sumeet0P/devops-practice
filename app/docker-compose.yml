services:
  server:
    image: sumeet0p/server:latest
    ports:
      - "5000:5000"
    environment:
      - PORT=5000  
    restart: always 

  client:
    image: sumeet0p/client:latest
    ports:
      - "5173:80"
    depends_on:
      - server
    restart: always  